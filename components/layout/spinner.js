import React from 'React'

import { Container, Header, Content, Spinner } from 'native-base';

export default () => (
	<Container>
        <Header />
        <Content>
        	<Spinner />
        </Content>
    </Container>
)
